//Add to this file any modules that are used across many of your component bundles.

const React = require("react"),
    ReactDom = require("react-dom"),
    FinsembleControls = require("@chartiq/finsemble-react-controls"),
    async = require("async");

module.exports = {
    React, ReactDom, FinsembleControls, async
}
